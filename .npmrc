# .npmrc del proyecto
# NOTA: 'prefix' no puede setearse desde project config (error de npm)
# NOTA: 'PLAYWRIGHT_BROWSERS_PATH' es variable de entorno, no config de npm
# Si necesitás Playwright, agregala en .env o en el script de CI/CD como:
#   export PLAYWRIGHT_BROWSERS_PATH=0

legacy-peer-deps=true
